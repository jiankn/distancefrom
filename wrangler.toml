"$schema" = "./node_modules/wrangler/config-schema.json"

# Cloudflare Workers source config. Astro emits the final deploy config to dist/server/wrangler.json at build time.
name = "distancefrom"
compatibility_date = "2026-03-17"
